<Window x:Class="UsageTrackerNative.Modules.Settings.ImportOptionsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Import.Title}"
        Width="920"
        Height="720"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        WindowStyle="None"
        AllowsTransparency="True"
        MinWidth="820"
        MinHeight="640"
        ShowInTaskbar="False"
        Background="Transparent"
        FontFamily="Microsoft YaHei UI"
        FontSize="14"
        Foreground="{DynamicResource PrimaryTextBrush}"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        KeyDown="Window_KeyDown"
        Loaded="Window_Loaded">
    <Window.Resources>
        <Style x:Key="DialogButtonStyle" TargetType="Button">
            <Setter Property="MinWidth" Value="96" />
            <Setter Property="Height" Value="36" />
            <Setter Property="Padding" Value="14,0" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="Background" Value="{DynamicResource ButtonBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource DataGridRowHoverBrush}" />
                                <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource InputHoverBorderBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrimaryDialogButtonStyle" TargetType="Button" BasedOn="{StaticResource DialogButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource AccentRedBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource AccentRedBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource AccentTextBrush}" />
        </Style>
        <Style x:Key="OptionCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="14" />
            <Setter Property="Padding" Value="12" />
        </Style>
        <Style x:Key="PreviewDataGridColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="Background" Value="{DynamicResource ButtonBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
            <Setter Property="BorderThickness" Value="0,0,1,1" />
            <Setter Property="Padding" Value="8,7" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridColumnHeader">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center" RecognizesAccessKey="True" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border x:Name="DialogRoot" Margin="10" Opacity="0" Background="{DynamicResource WindowBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="22">
        <Border.RenderTransform>
            <TranslateTransform x:Name="DialogTransform" Y="-8" />
        </Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="18" ShadowDepth="0" Opacity="0.28" />
        </Border.Effect>
        <Grid Margin="22,20,22,18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock x:Name="TitleText" Text="{DynamicResource Import.Title}" FontSize="20" FontWeight="Black" />
                    <TextBlock x:Name="SubtitleText" Margin="0,6,0,0" Foreground="{DynamicResource SecondaryTextBrush}" TextTrimming="CharacterEllipsis" />
                </StackPanel>
                <Button Grid.Column="1" Content="×" Width="32" Height="32" Padding="0" FontSize="18" FontWeight="SemiBold" Click="CancelButton_Click" Style="{StaticResource DialogButtonStyle}" />
            </Grid>

            <Grid Grid.Row="1" Margin="0,18,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="10" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="10" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="10" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Border Style="{StaticResource OptionCardStyle}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Import.RecordCount}" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" />
                        <TextBlock x:Name="TotalRecordsText" Margin="0,6,0,0" FontSize="20" FontWeight="Black" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="2" Style="{StaticResource OptionCardStyle}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Import.NewConflicts}" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" />
                        <TextBlock x:Name="DiffText" Margin="0,6,0,0" FontSize="20" FontWeight="Black" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="4" Style="{StaticResource OptionCardStyle}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Import.CategoryRules}" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" />
                        <TextBlock x:Name="RulesText" Margin="0,6,0,0" FontSize="20" FontWeight="Black" />
                    </StackPanel>
                </Border>
                <Border Grid.Column="6" Style="{StaticResource OptionCardStyle}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Import.TimeRange}" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" />
                        <TextBlock x:Name="RangeText" Margin="0,6,0,0" FontSize="13" FontWeight="SemiBold" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
            </Grid>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel>
                    <Border x:Name="DataOptionsPanel" Style="{StaticResource OptionCardStyle}" Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource Import.DataProcessing}" FontWeight="Black" Margin="0,0,0,10" />
                            <ComboBox x:Name="DataModeBox" Height="36" SelectionChanged="Options_SelectionChanged" />
                            <TextBlock Text="{DynamicResource Import.ConflictStrategy}" Margin="0,12,0,6" Foreground="{DynamicResource SecondaryTextBrush}" />
                            <ComboBox x:Name="ConflictModeBox" Height="36" SelectionChanged="Options_SelectionChanged" />
                        </StackPanel>
                    </Border>

                    <Border x:Name="SettingsOptionsPanel" Style="{StaticResource OptionCardStyle}" Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource Import.ConfigProcessing}" FontWeight="Black" Margin="0,0,0,10" />
                            <ComboBox x:Name="SettingsModeBox" Height="36" SelectionChanged="Options_SelectionChanged" />
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource OptionCardStyle}">
                        <StackPanel>
                            <TextBlock Text="{DynamicResource Import.ConfigSummary}" FontWeight="Black" Margin="0,0,0,8" />
                            <TextBlock x:Name="ConfigSummaryText" Foreground="{DynamicResource SecondaryTextBrush}" LineHeight="21" TextWrapping="Wrap" />
                            <TextBlock Text="{DynamicResource Import.PreviewOnly}" Margin="0,10,0,0" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" LineHeight="18" TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>
                    </StackPanel>
                </ScrollViewer>

                <Border Grid.Column="2" Style="{StaticResource OptionCardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="110" />
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="SearchBox" Height="36" VerticalContentAlignment="Center" Padding="12,0" TextChanged="SearchBox_TextChanged" />
                            <ComboBox x:Name="SearchModeBox" Grid.Column="1" Margin="8,0,0,0" Height="36" SelectionChanged="SearchBox_TextChanged" />
                        </Grid>
                        <DataGrid x:Name="PreviewGrid" Grid.Row="1" Margin="0,12,0,0" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" CanUserAddRows="False" CanUserDeleteRows="False" EnableRowVirtualization="True" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Auto" ColumnHeaderStyle="{StaticResource PreviewDataGridColumnHeaderStyle}" Background="{DynamicResource PanelBrushAlt}" Foreground="{DynamicResource PrimaryTextBrush}" BorderBrush="{DynamicResource InputBorderBrush}" GridLinesVisibility="All" HorizontalGridLinesBrush="{DynamicResource InputBorderBrush}" VerticalGridLinesBrush="{DynamicResource InputBorderBrush}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="{DynamicResource Import.Column.Start}" Binding="{Binding StartText}" Width="110" />
                                <DataGridTextColumn Header="{DynamicResource Sessions.Column.Process}" Binding="{Binding ProcessName}" Width="120" />
                                <DataGridTextColumn Header="{DynamicResource Import.Column.Title}" Binding="{Binding WindowTitle}" Width="*" />
                                <DataGridTextColumn Header="{DynamicResource Sessions.Column.Subject}" Binding="{Binding SubjectText}" Width="100" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </Grid>

            <Grid Grid.Row="3" Margin="0,16,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <CheckBox x:Name="DangerConfirmBox" VerticalAlignment="Center" Visibility="Collapsed" Foreground="{DynamicResource SecondaryTextBrush}" Checked="Options_SelectionChanged" Unchecked="Options_SelectionChanged">
                    <TextBlock Text="{DynamicResource Import.ConfirmReplace}" TextWrapping="Wrap" />
                </CheckBox>
                <Button Grid.Column="1" Content="{DynamicResource Dialog.Cancel}" Click="CancelButton_Click" Style="{StaticResource DialogButtonStyle}" />
                <Button x:Name="ApplyButton" Grid.Column="2" Margin="10,0,0,0" Content="{DynamicResource Import.Execute}" Click="ApplyButton_Click" Style="{StaticResource PrimaryDialogButtonStyle}" />
            </Grid>
        </Grid>
    </Border>
</Window>
